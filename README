#### Need create .env file in deric†ory project add litterals API_KEY - key service etherscan.io and PORT 
### Start project need enter commands in console:
*    npm i - install dependency
*    npm run build
*    npm run start - start project


### Detail realization:
*   BlockService - implements the behavior of the GET request to get a block by number. 
*   AddressMoreChangeService - implements task logic.